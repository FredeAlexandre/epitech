- Est ce que en STDIN il faut ignorer exit ?
- La precision doit etre garder ou on doit retirer les 0 de fin ?
Example:
push int8(0)
push float(42.42)
mul
assert float(0.0)
; ou
assert float(0.00)
- Les floats ont forcement besoins d'un . dans le nombre ?